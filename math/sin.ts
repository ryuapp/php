/**
 * Returns the sine of num in radians.\
 * The num parameter is in radians.
 *
 * @param {number} num An angle in radians
 * @return {number} The sine of num in radians
 */
export function sin(num: number): number {
  return Math.sin(num);
}
