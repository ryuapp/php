/**
 * Returns the tan of num in radians.\
 * The num parameter is in radians.
 *
 * @param {number} num An angle in radians
 * @return {number} The tan of num in radians
 */
export function tan(num: number): number {
  return Math.tan(num);
}
