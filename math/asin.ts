/**
 * Returns the arc sine of num in radians.\
 * The num parameter is in radians.
 *
 * @param {number} num An angle in radians
 * @return {number} The arc sine of num in radians
 */
export function asin(num: number): number {
  return Math.asin(num);
}
