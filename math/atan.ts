/**
 * Returns the arc tangent of num in radians.\
 * The num parameter is in radians.
 *
 * @param {number} num An angle in radians
 * @return {number} The arc tangent of num in radians
 */
export function atan(num: number): number {
  return Math.atan(num);
}
